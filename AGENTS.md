# Working agreement

Read INITIAL_PLAN.md. The manager owns integration. Use TypeScript/Node; keep the engine deterministic and independent of providers and interfaces. Content is data and choices come from the engine.

Default bounded subagent: gpt-5.6-luna with max reasoning. Respect assigned file ownership. Report commands run, limitations, and elapsed time. Never fabricate live play, interviews, model identity, cost, or completion.

Run `npm run verify` before publishing behavior changes. Do not weaken checks to admit regressions. Keep original run evidence outside the repository and reference it from docs. Never give builders' context, files, traces, or tools to blind players.

Keep current priorities and evidence in docs/STATUS.md. The complete user goal remains larger than any initial slice.
