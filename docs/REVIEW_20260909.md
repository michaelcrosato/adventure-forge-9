# September 9 repository review

The browser could start or load another journey while an earlier startup,
choice or file read was pending. Each response writes the active checkpoint,
so response order could replace the journey the player intended to keep.
`src/player/web.ts` now disables journey controls during an operation and
guards the new/load handlers themselves. An expired session without a
checkpoint still starts one replacement journey; a failed recovery retains
the existing save download without enabling unavailable game actions.

Four new tests in `tests/browser-session.test.ts` execute the shipped browser
script with delayed network/file promises and DOM event doubles. They cover
overlapping startup/choice operations, failed and successful loads, expired
sessions and recovery failure. These are executable interaction regressions,
not a rendered browser check or an authentic playtest. No engine or authored
content changed.

The worktree was clean at `31044071b75880ac4da976c641079f6605b47f78` before
the browser changes. `npm run verify` first built successfully, then the
sandbox denied the test runner's IPC socket. That attempt is preserved. An
approved unrestricted rerun built successfully and passed 123 of 124 tests,
including all four new tests; none were skipped. The sole failure remains
`Audit exceeded 250000 conserved-parameter families; exhaustive coverage is
not established`, from `tests/scenario.test.ts`. Runtime was 137.37 seconds,
maximum RSS 2,574,904 KiB. The two changed source files matched their recorded
checksums after verification. Full verification and Stage 8 acceptance remain
open, and these local changes have not been published.

The original verification logs, tested source files, checksums, review report
and normalized worker response are outside the repository at
`/home/micha/.local/share/adventure-forge-9/af9-review-20260909/`. The sibling
`af9-review-20260909.sha256` indexes the original staged files; its absolute
`/tmp/af9-review-20260909/` prefix records their staging location. The durable
directory preserves those filenames. This is local evidence, not an external
attestation. The review began at 19:55:22 UTC and observed the completed full
verification at 20:09:51 UTC, including time waiting for sandbox approval.

The previous proof cannot be treated as running: its session handle returns
`Unknown process id 20888`, and the recorded temporary checkouts are missing.
The v6 generator, manifest and finalization paths also fail `stat`. A search
under the durable evidence root found no `cone-*.json.gz`, `*failure-candidate*`
or `*candidates-v6*` files. This does not invalidate older archived evidence,
but it supplies no verdict for the lost latest attempt. Git retains the
certificate release source at `4a78944`; recovering that code and rebuilding
reproducible candidate generation is the next audit task. The existing audit
guard and all gameplay distinctions remain required.

One bounded reviewer was requested as `gpt-5.6-luna` with `max` reasoning to
inspect the Stage 8 regional content and its regression tests. The manager
rejected both proposed findings after checking the actual contract and copy.
The internal `explore-reedway-before-blackglass` ID has a phase-neutral public
label and intentionally remains available after Blackglass. Debt four is a
threshold on particular Reedway credit offers, not a specified global cap.
No content changes or new live players followed that review. The worker
reported approximately one second of shell-command time; total worker elapsed
time and monetary cost were not measured.

Commands used: `cat`, bounded `sed`/`tail`, `rg`, `find`, `stat`, `ps`,
`git status --short --branch`, `git worktree list --porcelain`, `git ls-tree`,
`git show`, `git diff --name-status`, `npm run verify`, `sha256sum` and `cp`.
The manager owns the browser implementation, verification and disposition.
The broader persistent-world goal remains active.
